import { pageMetadata } from "@/lib/page-metadata";

export const metadata = pageMetadata("ui-library-parity");

export default function UiLibraryParityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
